module G = Graphics

let black = G.black
let white = G.white
let border = 0x4444FF
let text = 0x111111
let button_text = 0xDDDDFF
let button_bg = 0x2222FF

(* Used for testing in order to indicate that a color should not be rendered
   onto the screen. *)
let failure = G.magenta
