module G = Graphics

let black = G.black
let white = G.white
let border = 0x444444
let text = 0x111111

(* Used for testing in order to indicate that a color should not be rendered
   onto the screen. *)
let failure = G.magenta
